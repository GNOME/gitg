install_data(
  'gitg.svg',
  rename: '@0@.svg'.format(application_id),
  install_dir: join_paths(gitg_datadir, 'icons', 'hicolor', 'scalable', 'apps'),
)

install_data(
  'gitg-symbolic.svg',
  install_dir: join_paths(gitg_datadir, 'icons', 'hicolor', 'scalable', 'apps'),
)
